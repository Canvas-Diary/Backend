key:
  openai:
    api-key: ${DALLE_REST_API_KEY}
  gemini:
    api-key: ${GEMINI_REST_API_KEY}

cloud:
  aws:
    s3:
      bucket: canvasdiary
    stack:
      auto: false
    region:
      static: us-east-1
    credentials:
      access-key: ${S3_ACCESS_KEY}
      secret-key: ${S3_SECRET_KEY}